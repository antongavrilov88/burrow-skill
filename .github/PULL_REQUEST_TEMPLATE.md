Closes #

## What changed

## Checked
- [ ] `python3 -m py_compile scripts/*.py` and `bash -n` on the installers pass
- [ ] No `params.json`, tokens, keys or real IPs in the diff
- [ ] If an installer changed: re-ran it on a fresh server (which provider?)
- [ ] CHANGELOG.md updated under Unreleased
